# 实施进度与恢复断点

## 2026-08-08：主题、今日待办与日期选择批次

- 内置七套主题采用完整且互异的 token 组，覆盖场景层、面板、卡片、悬浮、双强调、文字、边框、危险色和阴影；墨石为明确深色主题。
- 全局主题编辑使用临时预览层：设置打开时预览优先，关闭后恢复项目覆盖；全局选择仍即时持久化。
- 自定义主题改为稳定 ID 的多主题集合，支持新增、编辑、复制、应用、删除与明确回退；背景复制到受管目录并通过安全协议解析。
- “今日待办”改为本地日历日截止窗口查询，默认 3 天、范围 0–30；任务卡显示窗口外、临近、今天、超时四态标签。
- 修复日期选择器随快捷菜单卸载的问题；提醒使用日期时间，截止使用仅日期，均 Teleport 到顶层并进入统一任务自动保存。

本批不改变项目整体 `in-progress` 状态；Windows 安装包、完整人工回归以及更深入的可访问性审计仍待后续完成。

## 阶段

- [x] 项目与文档基线、Git 本地初始化
- [x] Task v1 编解码、文件仓储、原子写入、revision 冲突测试
- [x] Electron 安全 IPC 与三栏基础 UI；启动读取、新增、完成、重要、标题和备注保存
- [x] 工作目录 JSON、项目/子项目文件夹配置；项目创建、改名和安全归档
- [x] 今日待办、收藏、计划内、已完成查询与全局搜索
- [x] 子任务独立 Markdown 持久化、父子查询、改名、收藏、完成/恢复；旧 steps 等待 task@2 迁移
- [x] 修复 sandbox preload 失效导致的内存假新增；真实 Electron 冒烟验证任务/分组落盘
- [x] 无原生标题栏、自绘窗口控制区；未选择任务时详情栏不渲染且不占列宽
- [x] 应用内项目新增/重命名模态框，替代 Electron 不支持的 window.prompt
- [x] 账号菜单、设置面板、本机 settings.json、全局主题和校验后删除旧目录的工作目录迁移
- [x] 默认项目是真实文件夹；旧“任务”默认项目启动时安全改名
- [x] 项目树折叠、双击原位改名、项目属性、移动与标题菜单；项目主题持久化
- [x] 子任务进入中间任务列表，父任务支持逐层展开/折叠；任务详情可主动关闭
- [x] 项目空白区右键新增、归档任务查看与未完成任务归档警告
- [x] 项目视图设置的全局默认、祖先继承、自行维护与重设继承
- [x] 当前列表查询、九维任务统计、子项目目录显示与双击跳转
- [x] 任务详情截止时间、提醒调度、重复配置、简单/高级 Markdown 备注
- [x] 附件复制、路径边界校验、格式卡片、内置预览、另存为与移除
- [x] IPC 纯 DTO、PNG 响应式附件预览回归门禁与可关闭错误条
- [x] 任务详情防抖串行自动保存、flush、状态提示与 revision 冲突草稿选择
- [x] 高级详情固定占窗口 50%，Milkdown/Crepe Markdown-first 编辑器带固定格式工具栏、明确输入边框和紧凑内边距
- [x] 任务展开与项目展开独立设置模型及旧配置迁移
- [ ] 提醒、重复实例、附件与恢复中心
- [ ] Windows 安装包、视觉回归与人工验收

## 边界

当前完成项目组织、主题和任务层级交互批次，仍不是完整 MVP。自动验证结果记录在 `development-log.json`。下一批从截止日期、提醒与重复领域语义开始，并继续准备隔离验收数据目录。

## 2026-08-06 项目交互与主题批次

工作目录迁移按产品决定改为“复制、校验、删除旧目录、切换”，不再保留恢复副本。项目配置新增图标、描述、侧边栏颜色、主题和折叠状态；项目可在标题菜单中改名、移动、排序、换主题和归档，右键属性可集中编辑。侧边栏项目树可折叠、双击原位改名；账号入口移至侧边栏底部。子任务作为独立任务出现在中间列表，并由父任务控制展开。视觉采用浅绿雾面、半透明面板和柔和层次，未复制参考产品素材。

## 2026-08-07 主布局回归修复

真实主界面复现发现 CSS Grid 将侧边栏宽度 `278px` 误写进了行定义，导致侧边栏只有约 278px 高、账号入口悬在页面中部并产生整窗滚动。现改为固定 42px 标题栏与 `minmax(0, 1fr)` 内容行；未选任务时详情轨道为 0，选中任务时才扩展为 340px。Electron 冒烟新增运行时尺寸门禁：1280×800 窗口中侧边栏高度必须超过 700px且不越界、主列表宽度超过 900px、body 不得产生额外滚动。

## 2026-08-07 图标、字体与点击热区

移除界面功能控件中的字符图标，改为项目内统一 SVG 图标组件：24×24 viewBox、1.8px 圆角描边，导航、窗口按钮、任务、详情和菜单共用同一视觉规则；默认项目的文件夹 Emoji 也替换为 SVG，用户自行设置的项目图标仍按原值显示。字体改为优先使用 Windows 的 `Segoe UI Variable Text`，中文由 `Microsoft YaHei UI` 回退，标题使用 `Segoe UI Variable Display`。所有按钮、任务卡片、项目行和色板均显示手形光标；项目与任务展开热区分别扩大到 34×36 和 32×32，并提供 hover 与键盘焦点反馈。Electron 冒烟会检查展开热区尺寸及关键控件的计算 cursor。

## 2026-08-07 项目头部、统计与主题设置

项目树每行显示“未完成顶级任务 / 未完成全部任务”，项目头部同步显示带文字的两项统计。项目标题改为从根项目到当前项目的可点击面包屑，日期和描述移到其下方，统计与更多按钮在同一操作基线。设置新增 220–380px 侧栏宽度，新增林间、海岸两套随应用打包的本地 SVG 图片主题，以及自定义强调色、背景色和本地背景图片。项目菜单复用全部内置主题，并在全局自定义主题存在时提供“自定义”入口；选择只更新当前项目的 `.bearai-project.json`。Electron 冒烟验证两级面包屑、统计文案以及从项目菜单选择林间主题后 `theme-forest` 实际生效。

## 2026-08-07 归档、查询与项目视图继承

项目树空白区域新增右键菜单，可直接创建顶级项目。账号菜单新增归档入口，归档设置页读取 `.archive/projects` 中的项目配置和任务 Markdown，按未完成、已完成与全部汇总并允许展开查看任务标题；归档项目及子项目存在未完成任务时先显示数量警告并要求确认。

任务列表头部新增仅作用于当前视图的查询框，以及未完成、已完成、全部三组统计；每组数字依次表示“当前项目顶级 / 当前项目全部 / 当前项目及子项目全部”。项目菜单新增默认不展开、默认展开、记住上次展开情况、默认展开层级和显示子项目目录。显示子项目后，中间列表以目录卡片列出直接子项目并支持双击进入。

全项目默认设置保存于本机 `settings.json`，项目覆盖保存在自身 `.bearai-project.json`。子项目默认继承最近的自行维护祖先，否则继承全项目默认；项目菜单明确显示当前来源，自行维护后可重设为继承，顶级项目可重设为全局默认。设置窗口拆分为常规、外观、项目默认、归档四类，并修复更新主题时遗漏工作目录字段的问题。

## 2026-08-07 任务详情与附件批次

任务详情中的提醒、截止时间和重复由禁用占位改为真实控件并写入任务 Markdown；主进程在启动和任务保存后调度 Windows 本地通知。备注上方新增简单/高级任务单选按钮组，高级模式以 280ms 边界动画将详情栏从 340px 扩展到约 680px，并使用 Markdown 工具栏、源码与安全转义后的同屏预览。窄屏退化为右侧覆盖面板；高级面板打开时中间栏标题和查询分行、隐藏统计，避免横向溢出。

附件选择后复制进工作目录，frontmatter 只记录元数据和相对路径。附件卡片显示扩展名徽标、文件名、大小和 MIME；点击打开应用内预览弹窗，支持图片、音视频、PDF 和文本类文件，不支持格式显示说明。所有弹窗均提供另存为按钮，移除时删除受路径边界保护的附件实体并更新 Markdown。

## 2026-08-06 运行时缺陷修复

根因是 sandbox preload 使用 ESM 编译产物，bridge 未注入后渲染层又静默使用内存 mock，造成任务看似成功但重启丢失，分组则无反馈。现改为 sandbox 可执行的 CommonJS preload，增加健康检查并移除全部任务内存回退；bridge 失败会显示错误。Vite 生产资源改为相对路径，`npm start` 加载构建版，只有 `--dev` 才访问开发服务器。真实 Electron 冒烟已验证分组和任务可重新读取且对应 Markdown 文件存在。

## 2026-08-07 自动保存、Milkdown 与展开模型修正

preload 对 task、嵌套 attachment、project patch 与 settings patch 统一转换为 JSON 兼容纯 DTO，渲染调用附件操作前再次去除 Vue Proxy，修复 `An object could not be cloned`。任务文本采用 550ms 防抖、单队列串行保存；关闭详情、切换任务/项目和应用关闭按钮会 flush。界面显示未保存、正在保存、已保存、保存失败；失败不再调用 `load()` 覆盖草稿，revision 冲突同时保留本地和磁盘版本供选择。

高级详情宽度固定为窗口的 50%，窄窗口仍采用右侧覆盖层，列表区不产生横向滚动。高级编辑器为 Milkdown/Crepe，正文仍只写 Markdown；固定工具栏暴露标题、粗体、斜体、删除线、列表、引用、代码与撤销/重做，图片进入受管附件并通过安全协议显示。截止时间和提醒使用 Vue Datepicker，并在外层提供今天、明天、下周等快捷选择；保存格式仍为 ISO 8601。项目菜单分成独立“任务展开”和“项目展开”。任务展开又分为互斥默认策略 `defaultTaskExpansion.mode`（默认不展开或按固定层级展开）与独立记忆开关 `rememberTaskExpansion`；项目字段 `showSubprojects` 双向不联动。固定深度只有第一至第五层和全部。旧三态及前一错误迭代字段只在读取时迁移，不再写回。
