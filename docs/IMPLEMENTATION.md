# 实施进度与恢复断点

## 阶段

- [x] 项目与文档基线、Git 本地初始化
- [x] Task v1 编解码、文件仓储、原子写入、revision 冲突测试
- [x] Electron 安全 IPC 与三栏基础 UI；启动读取、新增、完成、重要、标题和备注保存
- [x] 工作目录 JSON、项目/子项目文件夹配置；项目创建、改名和安全归档
- [x] 今日待办、收藏、计划内、已完成查询与全局搜索
- [x] 子任务独立 Markdown 持久化、父子查询、改名、收藏、完成/恢复；旧 steps 等待 task@2 迁移
- [x] 修复 sandbox preload 失效导致的内存假新增；真实 Electron 冒烟验证任务/分组落盘
- [x] 无原生标题栏、自绘窗口控制区；未选择任务时详情栏不渲染且不占列宽
- [x] 应用内项目新增/重命名模态框，替代 Electron 不支持的 window.prompt
- [x] 账号菜单、设置面板、本机 settings.json、全局主题和校验后删除旧目录的工作目录迁移
- [x] 默认项目是真实文件夹；旧“任务”默认项目启动时安全改名
- [x] 项目树折叠、双击原位改名、项目属性、移动与标题菜单；项目主题持久化
- [x] 子任务进入中间任务列表，父任务支持逐层展开/折叠；任务详情可主动关闭
- [ ] 提醒、重复实例、附件与恢复中心
- [ ] Windows 安装包、视觉回归与人工验收

## 边界

当前完成项目组织、主题和任务层级交互批次，仍不是完整 MVP。自动验证结果记录在 `development-log.json`。下一批从截止日期、提醒与重复领域语义开始，并继续准备隔离验收数据目录。

## 2026-08-06 项目交互与主题批次

工作目录迁移按产品决定改为“复制、校验、删除旧目录、切换”，不再保留恢复副本。项目配置新增图标、描述、侧边栏颜色、主题和折叠状态；项目可在标题菜单中改名、移动、排序、换主题和归档，右键属性可集中编辑。侧边栏项目树可折叠、双击原位改名；账号入口移至侧边栏底部。子任务作为独立任务出现在中间列表，并由父任务控制展开。视觉采用浅绿雾面、半透明面板和柔和层次，未复制参考产品素材。

## 2026-08-06 运行时缺陷修复

根因是 sandbox preload 使用 ESM 编译产物，bridge 未注入后渲染层又静默使用内存 mock，造成任务看似成功但重启丢失，分组则无反馈。现改为 sandbox 可执行的 CommonJS preload，增加健康检查并移除全部任务内存回退；bridge 失败会显示错误。Vite 生产资源改为相对路径，`npm start` 加载构建版，只有 `--dev` 才访问开发服务器。真实 Electron 冒烟已验证分组和任务可重新读取且对应 Markdown 文件存在。
