# 熊智ToDo 领域词汇

## 工作目录

用户选择的熊智ToDo根文件夹，是项目、任务、附件、归档和恢复数据的边界。

## 项目

工作目录内承载任务的文件夹。项目可包含子项目；子项目与项目是同一种实体，只是目录层级不同。

## 任务

由独立 Markdown 文件承载的可执行事项，拥有稳定身份、状态和自身属性。

## 子任务

通过 `parentId` 归属于另一个任务的任务。子任务仍是完整任务，不是父任务文件中的步骤或检查项；它拥有自己的 Markdown 文件、状态、日期、提醒、重复、收藏和备注。

## 今日待办

工作目录内所有未完成的顶层任务。子任务在父任务详情中展示，不与父任务重复出现在顶层列表。

## 收藏

用户主动标记以便集中查看的任务，不表示优先级或紧急程度。

