# 熊智ToDo 领域语言

熊智ToDo 管理本地优先、可通过 Git 协作的项目与任务。

## 任务组织

**工作目录**：用户选择的熊智ToDo根文件夹，是项目、任务、附件、归档和恢复数据的边界。

**项目**：工作目录内承载任务的文件夹。项目可包含子项目；子项目与项目是同一种实体，只是目录层级不同。

**任务**：由独立 Markdown 文件承载的可执行事项，拥有稳定身份、状态和自身属性。

**子任务**：通过 `parentId` 归属于另一个任务的任务；仍拥有自己的 Markdown 文件、状态和属性。
_Avoid_: 步骤、检查项

**今日待办**：工作目录内所有未完成的顶层任务。

**收藏**：用户主动标记以便集中查看的任务，不表示优先级或紧急程度。

## 身份与协作

**本机身份**：当前设备上配置的姓名、邮箱和稳定 ID；首期唯一可选用户是自己。
_Avoid_: 账号、登录用户

**协作者**：被项目授权查看、分配任务或发表评论的身份；未来由共享服务或仓库成员映射产生。

**分配**：任务通过稳定身份 ID 指向一个或多个负责人；显示名称和邮箱不是任务身份主键。

**评论**：附属于任务的独立 Markdown 实体，可通过 `parentCommentId` 回复另一条评论。
_Avoid_: 备注、聊天消息

## 项目同步

**Git 边界项目**：直接绑定一个远程仓库与分支的项目；其全部子项目继承同一 Git 边界。

**同步**：在隔离工作区完成 fetch、合并、提交和 push 的可恢复过程，不等同于单次 push。
